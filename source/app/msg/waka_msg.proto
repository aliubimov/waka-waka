syntax = "proto3";

enum WakaFrameFlags {
    Ack = 0;
    Nack = 1;
}

message WakaFrame {
    int32 seq = 1;
    int32 ack = 2;
    int32 size = 3;
    WakaFrameFlags flags = 4;

    bytes payload = 5;
}

message WakaMessage {
    string device_name = 1;
    string text = 2;
}
